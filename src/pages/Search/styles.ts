import styled from 'styled-components';

export const Section = styled.section`
  max-width: 1700px;
  margin: 0 auto;
  padding: clamp(24px, 4vh, 34px) clamp(20px, 4vw, 40px) clamp(56px, 10vh, 96px);
`;
