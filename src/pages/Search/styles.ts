import styled from 'styled-components';

export const Section = styled.section`
  max-width: 1700px;
  margin: 0 auto;
  padding: clamp(20px, 3vh, 28px) clamp(20px, 4vw, 40px) clamp(40px, 7vh, 64px);
`;
